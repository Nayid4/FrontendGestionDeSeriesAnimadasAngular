import { BuscarActorPipe } from './buscar-actor.pipe';

describe('BuscarActorPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarActorPipe();
    expect(pipe).toBeTruthy();
  });
});
