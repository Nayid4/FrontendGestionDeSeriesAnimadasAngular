import { BuscarPeliculaPipe } from './buscar-pelicula.pipe';

describe('BuscarPeliculaPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarPeliculaPipe();
    expect(pipe).toBeTruthy();
  });
});
